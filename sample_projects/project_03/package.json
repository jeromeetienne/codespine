{
	"name": "api-brief",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"description": "Sample project 03 for ts-knowledge-graph: a multi-API client that exercises the external-API surface (ExternalAPI / CALLS_EXTERNAL).",
	"scripts": {
		"dev": "tsx ./src/main.ts",
		"test": "node --import tsx --test \"tests/**/*.test.ts\"",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/node": "^20.14.0",
		"tsx": "^4.16.0",
		"typescript": "^5.5.0"
	}
}
